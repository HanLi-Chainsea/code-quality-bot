# code-quality-bot
Self-hosted bake-off: PR-Agent (review) + Qodo Cover (TS test-gen) + Diffblue (Java test-gen), wired to MiniMax.
Milestone 1 = evaluate which tool to adopt for what. See ../ChainThings/docs/PRODUCT-DIRECTION.md.
Quickstart: `cp .env.example .env`, fill keys, then `bash scripts/setup.sh`.
